"use client";
import React from "react";
import Navigation from "@/components/Navigation";

const Header = () => {
  return (
    <section>
      <Navigation />
    </section>
  );
};

export default Header;
